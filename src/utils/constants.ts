/**
 * Central video configuration constants.
 * All values used across the project should reference these constants
 * rather than hardcoding values.
 */

/** Video width in pixels */
export const WIDTH = 1920;

/** Video height in pixels */
export const HEIGHT = 1080;

/** Frames per second */
export const FPS = 30;

/** Total duration of the video in frames (32 seconds × 30 FPS) */
export const DURATION_IN_FRAMES = 960;

/** Composition ID used by Remotion */
export const COMPOSITION_ID = "MainVideo";
